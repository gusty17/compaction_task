connector.name=oracle
connection-url=jdbc:oracle:thin:@oracle-xe:1521/XEPDB1
connection-user=${ENV:ORACLE_APP_USER}
connection-password=${ENV:ORACLE_APP_PASSWORD}

# Default is IGNORE: unsupported types (XMLTYPE) are omitted from DESCRIBE
# and SELECT *. Enable the setting below only to reproduce the unsupported
# conversion behavior documented in the README.


# unsupported-type-handling=CONVERT_TO_VARCHAR
