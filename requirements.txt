pyspark==3.5.5        # local_parsing/: matches the bank's spark-operator custom image (Spark 3.5.5 / Scala 2.12); also needs a JDK 11/17
oracledb>=2.0         # init-scripts/**/seed_*.py only (seeding Oracle) -- the Spark job reads Oracle via JDBC jars, not this
python-dotenv>=1.0    # loads the repo-root .env (Oracle + MinIO secrets), used by both
